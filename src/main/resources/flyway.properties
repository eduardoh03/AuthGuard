# Flyway Configuration for Maven Plugin
# These properties are used by Flyway Maven plugin (mvnw flyway:migrate)
# Spring Boot reads from application.properties instead

flyway.url=jdbc:postgresql://localhost:5436/idp_db
flyway.user=idp_user
flyway.password=idp_password
flyway.cleanDisabled=false
